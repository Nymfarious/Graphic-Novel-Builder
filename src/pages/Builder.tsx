// MeKu Storybook Builder - Builder Page
// The main page builder interface

import React from 'react';
import { PageBuilder } from '@/components/pagebuilder';

const Builder: React.FC = () => {
  return <PageBuilder />;
};

export default Builder;
